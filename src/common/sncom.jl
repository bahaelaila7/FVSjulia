# SNCOM.F77 — Southern variant specific variables
# Fortran COMMON /SECOM/ → module-level globals

ISEFOR::Int32 = Int32(0)   # forest code from FORKOD (without district)
KODIST::Int32 = Int32(0)   # district number
